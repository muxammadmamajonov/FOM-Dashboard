# Secrets — never commit
.env
worker/.dev.vars
worker/.dev.vars.*

# Cloudflare Worker build/dev artifacts
worker/node_modules/
worker/.wrangler/
worker/dist/
worker/*.tsbuildinfo

# Local-only business data (the real CEO dashboard sits at project root for
# manual testing; it must never enter git history).
/index.html

# OS / editor cruft
.DS_Store
.idea/
.vscode/
